using System.Collections;
using System.Collections.Generic;
using UnityEngine;


namespace JJ
{
    public class EnemyWeaponSlotManager : CharacterWeaponSlotManager
    {
        /*
        public override void GrantWeaponAttackingPoiseBonus()
        {
            float currentPoise = characterManager.GetTotalPoise();
            currentPoise +=
            characterManager.GetCharacterStatsManager().totalPoiseDefense += characterManager.GetCharacterStatsManager().offensivePoiseBonus;
        }

        public override void ResetWeaponAttackingPoiseBonus()
        {
            characterManager.GetCharacterStatsManager().totalPoiseDefense -= characterManager.GetCharacterStatsManager().offensivePoiseBonus;
        }
        */
    }
}

